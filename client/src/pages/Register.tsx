import {UserRegistration} from "../containers/UserRegistration";

/**
 * Register is a React page that serves as a container for user registration.
 * It renders the UserRegistration component, which handles the registration process.
 * @returns A React element for user registration.
 */
export function Register() {
    return (
            <UserRegistration/>
    )
}